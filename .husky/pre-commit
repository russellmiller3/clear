#!/bin/sh
# Pre-commit: branch discipline plus fast compiler tests.

branch="$(git branch --show-current)"
if [ "$branch" = "main" ]; then
  echo "ERROR: Branch discipline guard refused a direct commit on main."
  echo "Create a focused branch first: git switch -c feature/<name>"
  exit 1
fi

echo "Running compiler tests..."
node clear.test.js
