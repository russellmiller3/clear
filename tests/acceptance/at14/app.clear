build for javascript backend
database is local memory

create a Contacts table:
  name, required

when user calls POST /api/contacts sending contac:
  needs login
  new_contact = save contac as new Contac
  send back new_contact
