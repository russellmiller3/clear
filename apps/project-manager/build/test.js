#!/usr/bin/env node
// E2E tests -- auto-generated by Clear compiler
// Run: node test.js
// Requires: server running on localhost:3000

const BASE = process.env.TEST_URL || "http://localhost:3000";
let passed = 0, failed = 0;
let _emailCounter = 0;
let _uniqueCounter = 0;
function _uniqueEmail() { return "test" + (++_emailCounter) + "@example.com"; }
function _uniqueText(base) { return base + "_" + (++_uniqueCounter); }

// Note: for clean re-runs, delete clear-data.json before starting the server
// unique constraints may cause 500s if stale data exists from a previous run

// Generate a test auth token using the same runtime as the server
const auth = require("./clear-runtime/auth");
const TEST_TOKEN = auth.createToken({ id: 1, role: "admin", email: "test@test.com" });
const AUTH_HEADERS = { "Authorization": "Bearer " + TEST_TOKEN, "Content-Type": "application/json" };

// IDs created by setup steps, used by dependent tests
const createdIds = {};

async function test(name, fn) {
  try {
    await fn();
    passed++;
    console.log("PASS:", name);
  } catch (err) {
    failed++;
    console.log("FAIL:", name, "-", err.message);
  }
}

function assert(condition, msg) { if (!condition) throw new Error(msg); }

async function run() {
  // Setup: create a team so dependent tests have a valid team_id
  await test("SETUP: create team for FK dependencies", async () => {
    const r = await fetch(BASE + "/api/teams", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({"name":"Test value"})
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Setup team should return an id");
    createdIds["team"] = data.id;
  });

  // Setup: create a project so dependent tests have a valid project_id
  await test("SETUP: create project for FK dependencies", async () => {
    const r = await fetch(BASE + "/api/projects", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({"team_id":"Test value","name":"Test value"})
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Setup project should return an id");
    createdIds["project"] = data.id;
  });

  // Setup: create a task so dependent tests have a valid task_id
  await test("SETUP: create task for FK dependencies", async () => {
    const r = await fetch(BASE + "/api/tasks", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({"project_id":"Test value","title":"Test value"})
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Setup task should return an id");
    createdIds["task"] = data.id;
  });

  await test("GET /api/teams returns 200", async () => {
    const r = await fetch(BASE + "/api/teams");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/teams/:id returns 200 or 404", async () => {
    const r = await fetch(BASE + "/api/teams/1");
    assert(r.status === 200 || r.status === 404, "Expected 200 or 404, got " + r.status);
  });

  await test("GET /api/members returns 200", async () => {
    const r = await fetch(BASE + "/api/members");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/projects returns 200", async () => {
    const r = await fetch(BASE + "/api/projects");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/tasks returns 200", async () => {
    const r = await fetch(BASE + "/api/tasks");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/comments returns 200", async () => {
    const r = await fetch(BASE + "/api/comments");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/activity returns 200", async () => {
    const r = await fetch(BASE + "/api/activity", { headers: AUTH_HEADERS });
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/health returns 200", async () => {
    const r = await fetch(BASE + "/api/health");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("POST /api/teams with valid data returns 201", async () => {
    const payload = {"name":"Test value"};
    payload["name"] = _uniqueText("name");
    const r = await fetch(BASE + "/api/teams", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Response should have an id");
  });

  await test("POST /api/teams with no body returns 400", async () => {
    const r = await fetch(BASE + "/api/teams", { method: "POST", headers: { "Authorization": "Bearer " + TEST_TOKEN } });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/teams with empty required field returns 400", async () => {
    const r = await fetch(BASE + "/api/teams", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ name: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/members with valid data returns 201", async () => {
    const payload = {"team_id":"Test value","user_name":"Test value","email":"test@example.com"};
    payload["team_id"] = createdIds["team"];
    payload["user_name"] = _uniqueText("user_name");
    payload["email"] = _uniqueEmail();
    const r = await fetch(BASE + "/api/members", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Response should have an id");
  });

  await test("POST /api/members with no body returns 400", async () => {
    const r = await fetch(BASE + "/api/members", { method: "POST", headers: { "Authorization": "Bearer " + TEST_TOKEN } });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/members with empty required field returns 400", async () => {
    const r = await fetch(BASE + "/api/members", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ team_id: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/projects with valid data returns 201", async () => {
    const payload = {"team_id":"Test value","name":"Test value"};
    payload["team_id"] = createdIds["team"];
    payload["name"] = _uniqueText("name");
    const r = await fetch(BASE + "/api/projects", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Response should have an id");
  });

  await test("POST /api/projects with no body returns 400", async () => {
    const r = await fetch(BASE + "/api/projects", { method: "POST", headers: { "Authorization": "Bearer " + TEST_TOKEN } });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/projects with empty required field returns 400", async () => {
    const r = await fetch(BASE + "/api/projects", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ team_id: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/tasks with valid data returns 201", async () => {
    const payload = {"project_id":"Test value","title":"Test value"};
    payload["project_id"] = createdIds["project"];
    payload["title"] = _uniqueText("title");
    const r = await fetch(BASE + "/api/tasks", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Response should have an id");
  });

  await test("POST /api/tasks with no body returns 400", async () => {
    const r = await fetch(BASE + "/api/tasks", { method: "POST", headers: { "Authorization": "Bearer " + TEST_TOKEN } });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/tasks with empty required field returns 400", async () => {
    const r = await fetch(BASE + "/api/tasks", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ project_id: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/comments with valid data returns 201", async () => {
    const payload = {"task_id":"Test value","author":"Test value","body":"Test value"};
    payload["task_id"] = createdIds["task"];
    payload["author"] = _uniqueText("author");
    payload["body"] = _uniqueText("body");
    const r = await fetch(BASE + "/api/comments", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Response should have an id");
  });

  await test("POST /api/comments with no body returns 400", async () => {
    const r = await fetch(BASE + "/api/comments", { method: "POST", headers: { "Authorization": "Bearer " + TEST_TOKEN } });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/comments with empty required field returns 400", async () => {
    const r = await fetch(BASE + "/api/comments", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ task_id: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("DELETE /api/projects/:id without auth returns 401", async () => {
    const r = await fetch(BASE + "/api/projects/999", { method: "DELETE" });
    assert(r.status === 401, "Expected 401, got " + r.status);
  });

  await test("DELETE /api/tasks/:id without auth returns 401", async () => {
    const r = await fetch(BASE + "/api/tasks/999", { method: "DELETE" });
    assert(r.status === 401, "Expected 401, got " + r.status);
  });

  await test("POST /api/teams with name over max length returns 400", async () => {
    const r = await fetch(BASE + "/api/teams", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ name: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/members with user_name over max length returns 400", async () => {
    const r = await fetch(BASE + "/api/members", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ user_name: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/projects with name over max length returns 400", async () => {
    const r = await fetch(BASE + "/api/projects", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ name: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/tasks with title over max length returns 400", async () => {
    const r = await fetch(BASE + "/api/tasks", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ title: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/comments with author over max length returns 400", async () => {
    const r = await fetch(BASE + "/api/comments", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ author: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/teams strips unknown fields (mass assignment protection)", async () => {
    const payload = { ...{"name":"Test value"}, admin: true, role: "superuser" };
    payload["name"] = _uniqueText("name");
    const r = await fetch(BASE + "/api/teams", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.admin === undefined, "admin field should have been stripped");
    assert(data.role === undefined, "role field should have been stripped");
  });

  await test("GET /api/teams returns created records after POST", async () => {
    const r = await fetch(BASE + "/api/teams");
    const data = await r.json();
    assert(Array.isArray(data), "Expected array");
    assert(data.length > 0, "Expected at least one record (from earlier POST test)");
  });

  await test("GET / serves HTML", async () => {
    const r = await fetch(BASE + "/");
    assert(r.status === 200, "Expected 200, got " + r.status);
    const html = await r.text();
    assert(html.includes("<!DOCTYPE html>"), "Expected HTML document");
  });

  console.log("");
  console.log("Results:", passed, "passed,", failed, "failed");
  process.exit(failed > 0 ? 1 : 0);
}

run();