#!/usr/bin/env node
// E2E tests -- auto-generated by Clear compiler
// Run: node test.js
// Requires: server running on localhost:3000

const BASE = process.env.TEST_URL || "http://localhost:3000";
let passed = 0, failed = 0;
let _emailCounter = 0;
let _uniqueCounter = 0;
function _uniqueEmail() { return "test" + (++_emailCounter) + "@example.com"; }
function _uniqueText(base) { return base + "_" + (++_uniqueCounter); }

// Note: for clean re-runs, delete clear-data.json before starting the server
// unique constraints may cause 500s if stale data exists from a previous run

// Generate a test auth token using the same runtime as the server
const auth = require("./clear-runtime/auth");
const TEST_TOKEN = auth.createToken({ id: 1, role: "admin", email: "test@test.com" });
const AUTH_HEADERS = { "Authorization": "Bearer " + TEST_TOKEN, "Content-Type": "application/json" };

// IDs created by setup steps, used by dependent tests
const createdIds = {};

async function test(name, fn) {
  try {
    await fn();
    passed++;
    console.log("PASS:", name);
  } catch (err) {
    failed++;
    console.log("FAIL:", name, "-", err.message);
  }
}

function assert(condition, msg) { if (!condition) throw new Error(msg); }

async function run() {
  // Setup: create a client so dependent tests have a valid client_id
  await test("SETUP: create client for FK dependencies", async () => {
    const payload = {"name":"Test value","email":"test@example.com"};
    payload["email"] = _uniqueEmail();
    const r = await fetch(BASE + "/api/clients", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Setup client should return an id");
    createdIds["client"] = data.id;
  });

  await test("GET /api/clients returns 200", async () => {
    const r = await fetch(BASE + "/api/clients");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/clients/:id returns 200 or 404", async () => {
    const r = await fetch(BASE + "/api/clients/1");
    assert(r.status === 200 || r.status === 404, "Expected 200 or 404, got " + r.status);
  });

  await test("GET /api/invoices returns 200", async () => {
    const r = await fetch(BASE + "/api/invoices");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/invoices/:id returns 200 or 404", async () => {
    const r = await fetch(BASE + "/api/invoices/1");
    assert(r.status === 200 || r.status === 404, "Expected 200 or 404, got " + r.status);
  });

  await test("GET /api/line-items returns 200", async () => {
    const r = await fetch(BASE + "/api/line-items");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/payments returns 200", async () => {
    const r = await fetch(BASE + "/api/payments", { headers: AUTH_HEADERS });
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/audit returns 200", async () => {
    const r = await fetch(BASE + "/api/audit", { headers: AUTH_HEADERS });
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/reports/summary returns 200", async () => {
    const r = await fetch(BASE + "/api/reports/summary", { headers: AUTH_HEADERS });
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("GET /api/health returns 200", async () => {
    const r = await fetch(BASE + "/api/health");
    assert(r.status === 200, "Expected 200, got " + r.status);
  });

  await test("POST /api/clients with valid data returns 201", async () => {
    const payload = {"name":"Test value","email":"test@example.com"};
    payload["name"] = _uniqueText("name");
    payload["email"] = _uniqueEmail();
    const r = await fetch(BASE + "/api/clients", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Response should have an id");
  });

  await test("POST /api/clients with no body returns 400", async () => {
    const r = await fetch(BASE + "/api/clients", { method: "POST", headers: { "Authorization": "Bearer " + TEST_TOKEN } });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/clients with empty required field returns 400", async () => {
    const r = await fetch(BASE + "/api/clients", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ name: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/invoices with valid data returns 201", async () => {
    const payload = {"client_id":"Test value","invoice_number":"Test value"};
    payload["client_id"] = createdIds["client"];
    payload["invoice_number"] = _uniqueText("invoice_number");
    const r = await fetch(BASE + "/api/invoices", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Response should have an id");
  });

  await test("POST /api/invoices with no body returns 400", async () => {
    const r = await fetch(BASE + "/api/invoices", { method: "POST", headers: { "Authorization": "Bearer " + TEST_TOKEN } });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/invoices with empty required field returns 400", async () => {
    const r = await fetch(BASE + "/api/invoices", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ client_id: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/line-items with valid data returns 201", async () => {
    const payload = {"invoice_id":"Test value","description":"Test value","quantity":42,"unit_price":42};
    payload["invoice_id"] = _uniqueText("invoice_id");
    payload["description"] = _uniqueText("description");
    const r = await fetch(BASE + "/api/line-items", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.id, "Response should have an id");
  });

  await test("POST /api/line-items with no body returns 400", async () => {
    const r = await fetch(BASE + "/api/line-items", { method: "POST", headers: { "Authorization": "Bearer " + TEST_TOKEN } });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/line-items with empty required field returns 400", async () => {
    const r = await fetch(BASE + "/api/line-items", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ invoice_id: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("DELETE /api/clients/:id without auth returns 401", async () => {
    const r = await fetch(BASE + "/api/clients/999", { method: "DELETE" });
    assert(r.status === 401, "Expected 401, got " + r.status);
  });

  await test("DELETE /api/invoices/:id without auth returns 401", async () => {
    const r = await fetch(BASE + "/api/invoices/999", { method: "DELETE" });
    assert(r.status === 401, "Expected 401, got " + r.status);
  });

  await test("DELETE /api/line-items/:id without auth returns 401", async () => {
    const r = await fetch(BASE + "/api/line-items/999", { method: "DELETE" });
    assert(r.status === 401, "Expected 401, got " + r.status);
  });

  await test("POST /api/clients with name over max length returns 400", async () => {
    const r = await fetch(BASE + "/api/clients", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ name: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/invoices with invoice_number over max length returns 400", async () => {
    const r = await fetch(BASE + "/api/invoices", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ invoice_number: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/line-items with description over max length returns 400", async () => {
    const r = await fetch(BASE + "/api/line-items", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify({ description: "" })
    });
    assert(r.status === 400, "Expected 400, got " + r.status);
  });

  await test("POST /api/clients strips unknown fields (mass assignment protection)", async () => {
    const payload = { ...{"name":"Test value","email":"test@example.com"}, admin: true, role: "superuser" };
    payload["name"] = _uniqueText("name");
    payload["email"] = _uniqueEmail();
    const r = await fetch(BASE + "/api/clients", {
      method: "POST", headers: AUTH_HEADERS,
      body: JSON.stringify(payload)
    });
    assert(r.status === 201, "Expected 201, got " + r.status);
    const data = await r.json();
    assert(data.admin === undefined, "admin field should have been stripped");
    assert(data.role === undefined, "role field should have been stripped");
  });

  await test("GET /api/clients returns created records after POST", async () => {
    const r = await fetch(BASE + "/api/clients");
    const data = await r.json();
    assert(Array.isArray(data), "Expected array");
    assert(data.length > 0, "Expected at least one record (from earlier POST test)");
  });

  console.log("");
  console.log("Results:", passed, "passed,", failed, "failed");
  process.exit(failed > 0 ? 1 : 0);
}

run();