# DATAFLOW:
# ┌──────────┐  POST /api/todos  ┌──────────┐  save  ┌──────────┐
# │ Frontend │ ──────────────> │ Backend  │ ─────> │    DB    │
# │  (form)  │ <────────────── │ (server) │ <───── │ (memory) │
# └──────────┘  GET /api/todos  └──────────┘  query └──────────┘

build for web and javascript backend

# Database
database is local memory

create a Todos table:
  title, required
  completed, default false
  created_at_date, auto

# Backend
allow cross-origin requests

when user calls GET /api/todos:
  all_todos = get all Todos
  send back all_todos

when user calls POST /api/todos sending todo_data:
  validate todo_data:
    title is text, required, min 1, max 500
  new_todo = save todo_data as new Todo
  send back new_todo with success message

when user calls DELETE /api/todos/:id:
  requires auth
  delete the Todo with this id
  send back 'deleted' with success message

# Frontend
page 'Todo App' at '/':
  on page load get todos from '/api/todos'

  section 'Form' with style card_bordered:
    heading 'My Todos'
    'What needs to be done?' is a text input saved as a title
    button 'Add':
      send title to '/api/todos'
      get todos from '/api/todos'
      title is ''

  display todos as table showing title, completed with delete
