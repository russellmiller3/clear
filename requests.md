# Compiler & Runtime Requests

This is Meph's live backlog of compiler/runtime gaps hit while building apps.
Format per entry: **App / What I needed / Proposed syntax / Workaround / Error hit / Impact**.

**No open requests.** (2026-06-04 — every prior request was verified fixed and cleared:
components rendering nav children, multi-line `sending { }` JSON bodies, the `form:`
/ `form 'Title':` typo block, snake_case response keys like `ai_response`, and
`refresh X from '/api/...'` re-rendering its list. History is in git.)
