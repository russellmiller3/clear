{
  "id": "1776698045797-8nml7f",
  "task": "Build this app in Clear:\n\n**Rate-Limited API** (Level 7)\n\nBuild any CRUD API with rate limiting. POST /api/items creates items (name required). GET /api/items lists all. Apply rate limiting: max 5 requests per minute. The 6th request within a minute should return 429.\n\nStarting skeleton:\n```clear\nbuild for javascript backend\n\ncreate a Items table:\n  name, required\n\n# Your endpoints here\n# Hint: rate limit 5 per minute\n\n```\n\nThe app must pass these HTTP tests:\n  - POST /api/items with body {\"name",
  "started_at": 1776698045,
  "ended_at": 1776698112,
  "weak_assertion_count": 0,
  "red_step_observed": false,
  "final_source": "build for javascript backend\n\ndatabase is local memory\n\ncreate a Items table:\n  name, required\n\ncreate a RateLimitLog table:\n  endpoint, required\n  timestamp, auto\n\nwhen user sends item_data to /api/items:\n  validate item_data:\n    name must not be empty\n  \n  recent = get all RateLimitLog where endpoint is '/api/items'\n  count = length of recent\n  \n  if count is greater than or equal to 5:\n    send back 'Rate limit exceeded' with status 429\n  \n  create log_entry:\n    endpoint is '/api/items'\n  save log_entry as new RateLimitLog\n  \n  saved = save item_data to Items\n  send back saved with status 201\n\nwhen user calls GET /api/items:\n  recent = get all RateLimitLog where endpoint is '/api/items'\n  count = length of recent\n  \n  if count is greater than or equal to 5:\n    send back 'Rate limit exceeded' with status 429\n  \n  create log_entry:\n    endpoint is '/api/items'\n  save log_entry as new RateLimitLog\n  \n  items = get all Items\n  send back items\n"
}