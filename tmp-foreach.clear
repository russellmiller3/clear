build for web

create a Messages table:
  text, required
  author

page 'Chat' at '/':
  on page load:
    get messages from '/api/messages'

  for each msg in messages:
    section 'Message Card' with style card_bordered:
      heading msg's author
      text msg's text
