{
  "id": "multi-tenant",
  "level": 8,
  "title": "Multi-Tenant Workspaces",
  "description": "Users belong to workspaces. POST /api/workspaces creates a workspace. POST /api/workspaces/:id/members adds a user to a workspace. GET /api/workspaces/:id/items returns only items that belong to that workspace. Items are scoped to workspace — you can't see items from other workspaces.",
  "skeleton": "build for javascript backend\n\ncreate a Workspaces table:\n  name, required\n\ncreate a Members table:\n  workspace_id, required\n  user_id, required\n\ncreate a Items table:\n  title, required\n  workspace_id, required\n\n# Your endpoints here\n",
  "tests": [
    {
      "method": "POST",
      "path": "/api/workspaces",
      "body": { "name": "Team Alpha" },
      "expect": { "status": 201, "bodyHas": ["id"] }
    },
    {
      "method": "POST",
      "path": "/api/workspaces",
      "body": { "name": "Team Beta" },
      "expect": { "status": 201, "bodyHas": ["id"] }
    },
    {
      "method": "GET",
      "path": "/api/workspaces",
      "expect": { "status": 200, "bodyLength": ">0" }
    }
  ]
}
