# Handoff — 2026-04-25 (mid-day shipping session)

## Status right now

Tree on `main`. 16 commits ahead of `origin/main` on the morning push, +3 more this session = 19 commits total to push. All compiler tests green (`node clear.test.js` 2525 / 0). Tenant tests 75/0. MCP tests 161/0. Sweep harness tests pass. No production-Anthropic API spend this session — Ghost cc-agent only.

When Russell wakes / next session: `git push origin main`. Then read the two new plan files (LAE Phase C, Charts T2#8) — both have 4 small open decisions for him to sign off on before execution.

---

## What just shipped this session

- **Flywheel description fix.** MCP tool descriptions for `run_tests`, `list_evals`, `run_evals`, `run_eval`, `db_inspect` lied — they said "Not yet available in MCP mode" while the dispatcher and MCP context were fully wired. Meph in cc-agent mode read those descriptions and skipped the tools, so overnight Ghost sweeps produced zero Factor DB rows. Descriptions now reflect reality. Drift guard added in `mcp-server.test.js`.
- **GM-6 — sweep defaults to cc-agent.** `validateSweepPreconditions(env, opts)` now takes `opts.real`. Default behavior: route through cc-agent (no API spend). Pass `--real` to opt back into production Anthropic. Banner announces the default at sweep start.
- **CC-1 next — `PostgresTenantStore` interface stub.** Mirrors `InMemoryTenantStore`'s 17-method public surface. Every method throws `NOT_IMPLEMENTED` with the SQL the production version will run, so the future Phase 85a wire-up has a 1:1 shopping list. Contract test verifies surface parity.
- **LAE Phase D — LAE-8 audit log per app (write path).** `appendAuditEntry()` + `getAuditLog()` on `InMemoryTenantStore`. Append-only, no cap. Audit row schema: `{ts, actor, action, verdict, sourceHashBefore, sourceHashAfter, note}`. This is the accountability surface Phase C destructive ships will write to. 4 new TDD tests.
- **Cookies T2#42 marked DONE.** Investigation revealed the JS path is fully shipped (`set cookie`, `get cookie`, `set signed cookie`, cookie-parser auto-wired, secure-by-default). `requests.md` updated. Python path remains the only open piece.
- **`plans/plan-lae-phase-c-04-25-2026.md`** — full 7-cycle TDD plan for Phase C destructive ships + migration planner. Generated by a background plan agent. 4 open decisions Russell signs off on.
- **`plans/plan-charts-t2-8-04-25-2026.md`** — 6-cycle plan for the missing 4 chart types (donut, scatter, gauge, sparkline). **Key finding: bar/line/pie/area already work — `requests.md` T2#8 is stale.** Pure additive plan.

Detailed entries: `CHANGELOG.md` → `2026-04-25` section. Commits: `git log --oneline 1b07dd9..main`.

---

## ⚠️ Known issue — needs investigation next session

**Ghost cc-agent sweeps still produce zero Factor DB rows.** Verified after the description fix landed: 3-task sweep ran cleanly, 1 task said TC, 0 rows added. The MCP description fix unblocked Meph from skipping tools, but the **compile tool's Factor DB write isn't firing in cc-agent mode**. Hypothesis: `ctx.factorDB` may not be wired correctly in `playground/ghost-meph/mcp-server/tools.js`'s `buildMephContext()`, OR the `getFactorDb()` lazy initializer is failing silently. Investigation surface:

```
playground/ghost-meph/mcp-server/tools.js:158 — buildMephContext()
playground/ghost-meph/mcp-server/tools.js:131 — getFactorDb()
playground/meph-tools.js:992 — compile tool's logAction call
```

Fastest path: add a `console.error('[meph-tools] factorDB =', !!ctx.factorDB)` at the top of `compileTool`, run the sweep, see if it logs. Likely fix: `getFactorDb()` is finding `FACTOR_DB_PATH` but the SQLite open is failing. ~30 min to diagnose + fix.

Once fixed, every cc-agent sweep starts compounding the flywheel — the whole reason to fix this.

---

## Authorized for next session

Three plan-driven workstreams ready to fire:

1. **LAE Phase C** — `plans/plan-lae-phase-c-04-25-2026.md` has 7 cycles. ~4-6 hours agent work. Phases C+D were yesterday's "finish lae" ask; LAE-8 (Phase D's audit log write path) shipped today, leaving Phase C destructive ships + migration planner as the open piece. Decisions Russell needs to make before cycle 1 fires:
   - Confirmation phrase format: `DELETE field X` vs `DROP field X`
   - Audit log retention cap: 200 entries vs 1000 entries
   - Widget destructive button copy: "Permanently ship" vs "I understand — ship and destroy"
   - Audit-write failure mode: fire-and-forget vs block-ship-on-failure

2. **Charts T2#8 extension** — `plans/plan-charts-t2-8-04-25-2026.md` has 6 cycles. ~2-3 hours agent work. Adds donut, scatter, gauge, sparkline. The existing 4 types (bar/line/pie/area) already work. Almost no decisions — pure codegen.

3. **Fix the cc-agent Factor DB write bug** (above). Smallest fix, biggest leverage — every Ghost sweep after this lands compounds the flywheel for $0.

Plus the standing pulls from `ROADMAP.md` P0/P1:

- **CC-4 — Publish button wired to Clear Cloud.** Depends on CC-1 (multi-tenant routing scaffolding shipped this morning). 3 days.
- **GTM-2** `landing/marcus.html` and **GTM-5** Studio onboarding tweak. Static + UI work, $0 spend.
- **Builder Mode default flip.** Builder Mode becomes default for new users; cmd+. reveals 3-panel. 1 day.

**DO NOT do without explicit authorization:**
- Anything that spends production Anthropic API budget.
- Force pushes, branch deletions on `main` or `snapshot/*`.
- Strategic pivots — the Dave-first vs Marcus-first decision in ROADMAP is *Russell's* call.

---

## Maintenance rule for HANDOFF.md

Same as before. Move past content out per the routing table at the bottom of any prior HANDOFF. Cap ~150 lines. Rewrite "Status right now" + "What just shipped" + "Next priority" at end of every session.
