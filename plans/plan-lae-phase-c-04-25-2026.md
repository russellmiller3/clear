# Plan — LAE Phase C: Destructive Changes + Schema Migration Planner

**Date:** 2026-04-25
**Branch suggestion:** `feature/lae-phase-c-destructive`
**Scope:** Ship destructive edits via the widget (LAE-3) with the typed-confirmation gate, persisted audit log rows, cloud rollback parity, and a minimal schema-migration planner cycle (LAE-5 / PM-5 — rename only).
**Effort:** 7 cycles of ~30-45 min each.
**Reads through:** `lib/ship.js`, `lib/edit-api.js`, `lib/change-classifier.js`, `runtime/meph-widget.js`, `playground/tenants.js`, `playground/server.js`, `playground/deploy-cloudflare.js`, `lib/edit-tools.js`, `lib/edit-tools-phase-b.js`, `lib/proposal.js`.

> Generated by a Plan agent on 2026-04-25 after the LAE-8 audit log shipped.
> The audit log row schema (`actor`, `action`, `verdict`, `sourceHashBefore`,
> `sourceHashAfter`, `note`, `ts`) is what cycle 4 below writes into.

---

## What we found in the audit

1. The classifier already **labels** destructive verdicts (`remove_field`, `remove_endpoint`, `remove_page`, `change_type`, `require_without_default`, `remove_table`) — the verdict path is complete. There is **no propose-tool** for destructive changes; widget mode currently only exposes `propose_add_*` and `propose_hide_field` / `propose_rename_field`.
2. The widget today rejects non-additive proposals with a "Destructive — Phase C only" chip. The rendered `severityLabel` already exists. We add a typed-confirm flow on the destructive branch.
3. Cloud rollback already exists end-to-end (`/__meph__/api/cloud-rollback` → `applyCloudRollback` → `rollbackToVersion` → `recordVersion(via:'widget')` with note prefix `widget-undo-to-…`). **Destructive ships roll back through the same `versions[]` ladder — no new code path.** This is the key Phase B reuse.
4. There is no persisted audit-log table yet — but **LAE-8 (this session) shipped `getAuditLog` + `appendAuditEntry` on `InMemoryTenantStore`**. Phase C cycle 3 below extends the schema with the destructive-specific fields (kind, before/after diff, reason, ip, userAgent, versionId).
5. Compiler already emits `ALTER TABLE … ADD/DROP COLUMN` for `MIGRATION` nodes (compiler.js:7785-7814) — but no source-level node fires them today; existing CREATE TABLE diff-emit on init is enough for adds, and Phase C drops are handled by the same shape (we generate a one-shot migration SQL string at ship time, not a new node type).
6. **Blocker check (negative):** none. All primitives exist. The classifier, store, deploy mode-switch, cloud rollback, and parser support for `hidden`/`renamed to` are shipped. Phase C is pure assembly + UX + audit-row extension.

---

## Cycles

### Cycle 1 — Destructive propose tool: `propose_remove_field`

**Test (red, in `lib/edit-tools-phase-c.test.js`, new):**

```
proposeRemoveField on a Users table with `notes` field returns ok:true,
classification.type === 'destructive', exactly one change kind === 'remove_field'.
proposeRemoveField on an unknown field returns ok:false with 'not found' error.
proposeRemoveField on the only PK-like field (id) returns ok:false 'cannot remove'.
```

**Green (minimum):** create `lib/edit-tools-phase-c.js` exporting `proposeRemoveField({source, table, field})`. Reuse `findTableBlock` + `findFieldLine` from `edit-tools-phase-b.js` (extract to a shared module if cleaner). Splice the field's line out of `lines`, parse, classify. Return `{ok:true, newSource, diff:'- ${origLine}', classification}` when classifier says `destructive` AND the only kind is `remove_field`. Reject if classifier reports anything else (defense in depth — same pattern as `proposeHideField`).

**Files touched:** `lib/edit-tools-phase-c.js` (new), `lib/edit-tools-phase-c.test.js` (new).
**Depends on:** none. Pure-string manipulation + classifier.

---

### Cycle 2 — Wire destructive tools into proposal dispatcher

**Test (red, extend `lib/proposal.test.js`):**

```
applyProposal(source, 'propose_remove_field', {table, field}) → ok:true with classification.type 'destructive'.
TOOL_DEFINITIONS includes 'propose_remove_field' with description that says
'PERMANENT', 'data loss', and 'requires typed confirmation'.
```

**Green:** add `proposeRemoveField` and (stubs for cycle 4) `proposeDropEndpoint`, `proposeChangeType` to `TOOL_IMPLS` in `lib/proposal.js`. Append a `propose_remove_field` entry to `TOOL_DEFINITIONS`. Description copy must steer Meph: "PERMANENT — column dropped, all values destroyed. Owner must type confirmation. Prefer `propose_hide_field` unless the owner has explicitly used the words 'permanently' / 'forever' / 'wipe'."

**Files touched:** `lib/proposal.js`, `lib/proposal.test.js`.
**Depends on:** Cycle 1.

---

### Cycle 3 — Audit log destructive-row extension

**Test (red, extend `playground/tenants.test.js`):**

```
appendAuditEntry now also accepts {kind, before, after, reason, ip, userAgent, versionId}.
Rows surface those fields verbatim on getAuditLog.
recordAuditEntry on unknown (tenantSlug, appSlug) still returns {ok:false, code:'APP_NOT_FOUND'}.
getAuditLog returns auditLog newest-first; original storage stays insertion-order.
auditLog caps at MAX_AUDIT_PER_APP=200; oldest trimmed on overflow.
```

**Green:** extend the existing `appendAuditEntry` to accept the destructive-specific fields. Add `MAX_AUDIT_PER_APP = 200` + trim logic. Extend `getAuditLog` to return newest-first via shallow-copy + sort (matches `getAppRecord(versions)` pattern). **Schema lock-in** — `before` and `after` are intentionally tiny string snippets (the diff hunk only — never full source — so audit row stays bounded). `kind` mirrors the classifier's change kind (`remove_field`, `drop_endpoint`, `change_type`). The audit row is the accountability surface that replaces the data snapshot for GDPR/CCPA/HIPAA erasure.

**Files touched:** `playground/tenants.js`, `playground/tenants.test.js`.
**Depends on:** none — pure store work, runs in parallel with cycle 1 if you want.

---

### Cycle 4 — Destructive ship endpoint + audit write

**Test (red, extend `lib/edit-api.test.js`):**

```
POST /__meph__/api/ship with {newSource, classification:{type:'destructive'}, confirmation, reason}
WITHOUT confirmation → 400 {error:/confirmation required/i, expected:'DELETE field email'}.
WITH wrong confirmation 'delete email' → 400 {error:/confirmation mismatch/i}.
WITH correct 'DELETE field email' AND reason → calls deps.applyShip,
then calls deps.appendAuditEntry({actor:req.user.email, action:'ship', verdict:'destructive',
kind:'remove_field', before:'- notes (text)', after:'(removed)', reason, versionId:result.versionId}).
WITHOUT reason → 400 {error:/reason is required/i}.
```

**Green:** in `createEditApi`'s `/__meph__/api/ship` handler, before calling `deps.applyShip`, look at `req.body.classification` (the widget already round-trips this from `/propose`'s response). If `classification.type === 'destructive'`:

- require non-empty `req.body.reason`
- require `req.body.confirmation` to equal the canonical phrase derived from the first destructive change: e.g. `DELETE field <name>` for `remove_field`, `DELETE endpoint <method> <path>` for `remove_endpoint`, `DELETE page "<title>"` for `remove_page`, `COERCE <table>.<field> from <X> to <Y>` for `change_type`. Build this string with a `requiredConfirmation(classification)` pure helper, exported for the widget to import.
- after `applyShip` returns `ok:true`, call `deps.appendAuditEntry` with the full row. If `appendAuditEntry` is missing on `deps`, log a warning and do NOT fail the ship (audit write is fire-and-forget on the happy path; alarm-only when missing).
- return `{...result, audit:{ok}}` so the widget can show "audit row written" confirmation.

Add a small pure helper file `lib/destructive-confirm.js` exporting `requiredConfirmation(classification)` so the widget and the API agree. Test it directly.

**Files touched:** `lib/edit-api.js`, `lib/edit-api.test.js`, `lib/destructive-confirm.js` (new), `lib/destructive-confirm.test.js` (new).
**Depends on:** Cycle 3.

---

### Cycle 5 — Widget destructive UX (typed confirmation + reason)

**Test (red, in `lib/meph-widget-eval.js` — existing widget eval harness, or `runtime/meph-widget.test.js` if new):**

```
Given a proposal {classification:{type:'destructive', changes:[{kind:'remove_field', table:'Users', field:'email'}]}}:
  renderProposal returns DOM with:
    a chip 'Destructive · permanent'
    a red diff
    an <input id="clear-meph-confirm"> with placeholder 'DELETE field email'
    a <textarea id="clear-meph-reason"> required
    a 'Permanently ship' button DISABLED until input.value === requiredConfirmation AND textarea.value !== ''
On Ship click, the body POSTed to /__meph__/api/ship includes
  {newSource, classification, confirmation, reason}.
On 400 'confirmation mismatch', the input border turns red and the button stays disabled.
```

**Green:** in `runtime/meph-widget.js` `renderProposal`, replace the current "Phase A only ships additive changes" branch for destructive with a new section that:

- imports `requiredConfirmation` (inlined as a JS helper in the widget — small enough to dup)
- renders the typed-confirmation input + reason textarea + a clearly-styled red-bordered "Permanently ship" button
- adds keyup listeners to enable/disable the button based on the exact-match confirmation
- on click: POST to `/ship` with `{newSource, classification, confirmation:input.value, reason:textarea.value}` (cloud slugs already attached when present)
- on 200: addBot(`Permanently shipped. Audit row recorded.`); reload as today
- on 400 with `expected`: addBot the expected phrase as a hint, re-enable input

Keep the existing reversible flow (Phase B's chip and behavior) unchanged. Style adds: a `.clear-meph-btn-danger` class (red bg) so the destructive button is unmistakable.

**Files touched:** `runtime/meph-widget.js`. (No widget test runner is in place; gate this cycle on syntax-check + a manual `playground/test-destructive-widget.html` smoke if Russell wants — but the API-side gate in cycle 4 is the load-bearing safety check.)
**Depends on:** Cycle 4.

---

### Cycle 6 — Cloud destructive ship: reuse `_deployUpdate` (no new code path)

**Test (red, extend `lib/ship.test.js` and `playground/deploy-cloudflare.test.js`):**

```
ship.test.js: applyShip with classification.type='destructive' AND a cloud io
(getCloudRecord returns a record) calls io.shipToCloud unchanged.
The mode:'update' path already records a `versions[]` row — destructive ship gets the same row, just with via:'widget-destructive'.

deploy-cloudflare.test.js: deploySource({mode:'update', via:'widget-destructive', ...})
calls store.recordVersion with via:'widget-destructive'.
```

**Green:** the rollback code path is **identical** to Phase B (rollbackToVersion + recordVersion). The only delta is the `via` tag on the new version row. In `playground/server.js`'s `applyShip` closure, plumb a `via: classification.type === 'destructive' ? 'widget-destructive' : 'widget'` into the `deploySourceCloudflare` call. No new endpoint, no new SQL, no new method. Cloud rollback for destructive ships works through the same `versions[]` ladder — versions[1] still points at the pre-drop bundle on Cloudflare, which still has the column. **Note for Russell:** the SQLite ALTER TABLE DROP COLUMN runs on the new bundle's `applyMigrations`; the column itself is gone from D1 even after rollback. **Cloud rollback restores the code, NOT the dropped data.** This is consistent with the audit-trail-replaces-snapshot decision in the roadmap. The widget UI in cycle 5 must say this in the confirm prompt — see cycle 7.

**Files touched:** `playground/server.js`, `playground/deploy-cloudflare.js` (only the `via` pass-through, if not already), `lib/ship.test.js`, `playground/deploy-cloudflare.test.js`.
**Depends on:** Cycles 4-5.

---

### Cycle 7 — Migration planner: rename detection (the single LAE-5 cycle)

**Test (red, in `lib/migration-planner.test.js`, new):**

```
planRename({beforeProgram, afterProgram}) when after has a removed `email` field
AND a new `contact_email` field of the same type returns
{detected:'rename', from:'email', to:'contact_email', table:'Users',
 options:[{id:'keep', label:'Copy email values into contact_email, then drop email'},
          {id:'discard', label:'Drop email, leave contact_email empty'}]}.
planRename when after just removes a field (no plausible new field) returns {detected:null}.
planRename when types differ returns {detected:'rename', warning:/type mismatch/}.
```

**Green:** create `lib/migration-planner.js`. Pure function. Looks at the classifier's changes — if there's a `remove_field` AND an `add_field` on the same table with matching `fieldType`, propose rename. Returns the option list as data; the widget renders. Wire into `/__meph__/api/propose`'s response: when classifier returns destructive AND `planRename` matches, attach `migrationPlan` to the response. The widget's destructive UX (cycle 5) checks for `proposal.migrationPlan` and shows the rename radio buttons BEFORE the typed confirmation. If owner picks `keep`, the widget calls a new tool `propose_rename_field` (already exists from Phase B) instead of shipping the destructive diff. If `discard`, ship the destructive diff as-is. **This is the simplest possible LAE-5 — recognize a rename, offer two options, no split/coerce/archive yet.** PM-5's full negotiation lives behind this cycle as a Phase D-or-later expansion.

**Files touched:** `lib/migration-planner.js` (new), `lib/migration-planner.test.js` (new), `lib/edit-api.js` (one-line plumb of `migrationPlan` into `/propose` response), `runtime/meph-widget.js` (render `migrationPlan.options` as a radio group above the confirm input).
**Depends on:** Cycles 1-2 (for the destructive tool path) and Cycle 5 (for the widget destructive UX it extends).

---

## Sequencing & gates

| Cycle | Depends on | Parallel-safe? |
|-------|-----------|----------------|
| 1 — `proposeRemoveField` tool | — | Yes |
| 2 — Dispatcher wiring | 1 | After 1 |
| 3 — Audit log destructive extension | — | Yes (parallel with 1-2) |
| 4 — Ship endpoint gate + audit write | 2, 3 | After 2 + 3 |
| 5 — Widget destructive UX | 4 | After 4 |
| 6 — Cloud destructive ship | 4-5 | After 5 (small pass-through) |
| 7 — Migration planner (rename only) | 1-2, 5 | After 5 |

**Phase C gate:** all of `node lib/edit-tools-phase-c.test.js`, `node lib/proposal.test.js`, `node playground/tenants.test.js`, `node lib/edit-api.test.js`, `node lib/migration-planner.test.js`, `node lib/destructive-confirm.test.js`, `node lib/ship.test.js`, `node playground/deploy-cloudflare.test.js` green. `node clear.test.js` regression count unchanged.

## Open decisions for Russell

1. **Confirmation phrase format.** I picked `DELETE field <name>`, `DELETE endpoint <method> <path>`, `DELETE page "<title>"`, `COERCE <table>.<field> from X to Y`. Consider whether `DROP` reads better than `DELETE` for fields/endpoints; both are typeable, both are unambiguous.
2. **Audit log retention cap.** I picked 200 entries per app. Trade-off: 200 destructive ships per app is generous (a single app rarely sees more than ~10 destructive ships); going to 1000 costs ~50KB per app in memory and gives more history.
3. **Widget destructive button copy.** "Permanently ship" or "I understand — ship and destroy"? The latter is one extra reading-friction tick of safety.
4. **Audit write failure mode.** I made it fire-and-forget (warn but don't fail the ship). If we want stricter compliance, flip to "block ship if audit write fails" — a one-line change in cycle 4.

## Risks

| Risk | Mitigation |
|------|-----------|
| Owner types confirmation phrase wrong 3x and gives up | Show the expected phrase verbatim in the input placeholder (already in cycle 5 test). One-click copy button could come in Phase D. |
| Audit row written but ship fails | Cycle 4 writes audit AFTER `applyShip` returns ok:true, so this can't happen — the audit row is only ever for shipped destructions. |
| Cloud rollback restores code but not the dropped data | This is by design (per roadmap "no data snapshot on destructive delete"). Document loudly in the widget destructive prompt: "Rolling back will restore the field's column but not its values." Cycle 5 includes this copy. |
| Meph proposes destructive when owner meant hide | Tool description (cycle 2) steers toward `propose_hide_field`. Server-side gate is independent — even if Meph picks the destructive tool, owner still has to type the confirmation. |
| Two owners both ship destructive at same time | DeployLockManager covers this (Phase B). Second click returns 409. |

## Definition of done

Russell opens a CF-deployed todo app, types "permanently delete the notes field" in the widget, sees the destructive chip and a `DELETE field notes` placeholder, types it, types "user requested erasure under GDPR", clicks "Permanently ship". Within 3s the field is gone from the live site and the column is dropped from D1. He calls `/api/audit-log?appSlug=todo` (Phase D's read surface — out of scope here, but the rows are queryable) and sees the row with actor, ts, before/after, reason, versionId. He clicks Undo; the previous version's code comes back (column re-added by ALTER TABLE in the rollback bundle's migration), but the deleted row values are not recovered. The audit row remains.

---

## Critical files for implementation

- `lib/edit-tools-phase-c.js` (new — destructive propose tools)
- `lib/edit-api.js` (typed-confirmation gate + audit write on `/ship`)
- `playground/tenants.js` (extend `appendAuditEntry` schema + cap)
- `runtime/meph-widget.js` (destructive UX: typed-confirm input, reason textarea, danger button, rename radio)
- `lib/migration-planner.js` (new — rename detection for cycle 7)
